import 'package:flutter/material.dart';

class FamilyHeadForm extends StatelessWidget {
  const FamilyHeadForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Head Form"),),
    );
  }
}
